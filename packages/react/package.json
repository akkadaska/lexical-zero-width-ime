{
  "name": "@lexical-zero-width-ime/react",
  "version": "1.0.0",
  "description": "A Lexical editor plugin for React to fix Safari caret issues and IME conflicts using Zero Width No-Break Space.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daisuke Akazawa",
  "license": "MIT",
  "dependencies": {
    "@lexical/react": ">=0.12.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "lexical-zero-width-ime": "^1.0.0"
  }
}
